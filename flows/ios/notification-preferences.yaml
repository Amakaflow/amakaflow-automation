# iOS Notification Preferences Flow
# Tests notification settings: toggle types, quiet hours
#
# Run: maestro test flows/ios/notification-preferences.yaml

appId: com.myamaka.AmakaFlowCompanion

onFlowError:
  - takeScreenshot: artifacts/screenshots/ios-notification-preferences-error

---

- launchApp:
    clearState: true
    stopApp: true
    arguments:
      UITEST_MODE: "true"
      UITEST_AUTH_SECRET: "${UITEST_AUTH_SECRET}"
      UITEST_USER_ID: "${UITEST_USER_ID:-test-user-123}"
      UITEST_USE_FIXTURES: "true"
      UITEST_FIXTURES: "amrap_10min"
      UITEST_SKIP_ONBOARDING: "true"
      UITEST_SKIP_APPLE_WATCH: "true"

- extendedWaitUntil:
    visible:
      id: "home_screen"
    timeout: 15000

# ============================================
# Navigate to Notification Settings
# ============================================

- tapOn:
    text: "More"
    retryTapIfNoChange: true

- tapOn:
    text: "Settings"
    retryTapIfNoChange: true

- extendedWaitUntil:
    visible:
      id: "settings_screen"
    timeout: 10000

- scrollUntilVisible:
    element:
      text: "Notifications"
    direction: DOWN
    timeout: 10000
    speed: 25

- tapOn:
    text: "Notifications"
    retryTapIfNoChange: true

- extendedWaitUntil:
    visible:
      id: "notification_preferences_screen"
    timeout: 10000

- takeScreenshot: artifacts/screenshots/ios-notification-preferences-01-overview

# ============================================
# Toggle notification types
# ============================================

- assertVisible:
    id: "workout_reminders_toggle"

- tapOn:
    id: "workout_reminders_toggle"
    retryTapIfNoChange: true

- takeScreenshot: artifacts/screenshots/ios-notification-preferences-02-toggle-reminders

- assertVisible:
    id: "sync_alerts_toggle"

- tapOn:
    id: "sync_alerts_toggle"
    retryTapIfNoChange: true

- takeScreenshot: artifacts/screenshots/ios-notification-preferences-03-toggle-sync

- assertVisible:
    id: "coach_messages_toggle"
    optional: true

- tapOn:
    id: "coach_messages_toggle"
    optional: true
    retryTapIfNoChange: true

- takeScreenshot: artifacts/screenshots/ios-notification-preferences-04-toggle-coach

# ============================================
# Verify quiet hours
# ============================================

- scrollUntilVisible:
    element:
      id: "quiet_hours_section"
    direction: DOWN
    timeout: 10000
    speed: 25

- assertVisible:
    id: "quiet_hours_section"

- assertVisible:
    id: "quiet_hours_toggle"

- takeScreenshot: artifacts/screenshots/ios-notification-preferences-05-quiet-hours

# Tap quiet hours toggle
- tapOn:
    id: "quiet_hours_toggle"
    retryTapIfNoChange: true

- takeScreenshot: artifacts/screenshots/ios-notification-preferences-06-quiet-hours-toggled

# Final assertions
- assertVisible:
    id: "notification_preferences_screen"
- assertNotVisible:
    id: "error_view"
    optional: true
